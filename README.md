目的:
  クラウドマウント上の大量画像を ImageMagick 全デコードで破損判定し、
  通信断等の一過性エラーを除外するため、失敗ファイルだけをリトライして確定する。

経緯:
  拡張子は信用できず、全デコードが必要。
  クラウドマウントは途中で切れるため、1回の失敗=破損とは限らない。

次のステップ:
  1) scan_all_to_fail1.ps1 で fail_1.txt 作成
  2) retry_list.ps1 で fail_1→fail_2→fail_3… と繰り返す
  3) 最終的に残った fail_N を破損/恒常エラーとして扱う
